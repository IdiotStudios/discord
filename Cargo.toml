[package]
name = "discord"
version = "0.1.0"
edition = "2024"

[dependencies]
serenity = { version = "0.12", default-features = false, features = ["client", "gateway", "model", "rustls_backend", "framework"] }
tokio = { version = "1.40", features = ["macros", "rt-multi-thread"] }
dotenvy = "0.15"
